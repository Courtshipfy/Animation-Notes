# 面试精华概要

> 面向面试的一览速查：每个专题先用一句话概括，再拆成几个部分，每部分只放「能触发联想的提示词」。细节回到正文笔记核对。当前覆盖：KAI 地面状态（起步/循环/停步/折返）、ALS 相机系统与姿势混合、UE5 现代动画节点（全部节点）。

---

## 一、KAI Locomotion

### 0. 统一背景（起步/循环/停步/折返共享的地基）

**一句话**：Kai 不直接按"按了哪个键"选动画，而是把输入、速度、朝向规整为局部方向与运动信号，再由状态图选路线、状态函数按相位交棒。

- **三个运动信号**：`bIsMoving`（平面速度非零，还有残余滑行）、`bIsAccelerating`（平面加速度非零，还有移动输入）、`bIsPivoting`（速度与加速度近似反向 + 有 Pivot 资格）。三者不等价。
- **方向来源区分（关键）**：起步看**加速度方向**（玩家想往哪去）；循环/停止看**速度方向**（角色正在往哪滑）。
- **四向卡迪纳尔分类**：连续角度仍保留（`LocalVelocityDirection` 等），四向值只用于选资源，扇区内角度交给 Warping。
- **主线**：Idle →（加速）Start →（交棒）Loop →（松输入仍滑行）Stop → Idle；反向输入 → Pre/Post-Pivot → Loop。
- **职责两层**：状态图选路线（Start/Stop/Pivot/Idle）；状态函数写「出口就绪标记」（`StandingStartBlendToLoop` / `StandingStopBlendToLoop` / `StandingPostPivotBlendToLoop`），不是通用的"正在移动"布尔值。
- **靶向/非靶向**：差异在角色数据生成阶段就存在（`DesiredRotation` 取控制器旋转 vs `ComputeOrientToMovementRotation`），不是只在选动画最后一步。

---

### 1. 起步（Start）

**一句话**：起步连续回答三问——选哪段动画、从哪一帧开始播、每帧怎么推进；前提是**距离匹配驱动**（胶囊不动动画就停），不是按时间自由播放。

**① 选资源（Setup）**
- `StartDir` = 局部加速度方向 × `RotationDirection`（带正负号）；`StartCardinalDir` 按 10° 死区归前/后/左/右。
- 非靶向 + `HasRotatingStarts`：`|dir| ≤ 60°` 选四向 `StrafeStarts`；`>60°` 按符号选 90°（60~120°）/180°（≥120°）旋转起步。
- 靶向（或未配旋转起步）→ 直接四向。站立按 Walk/Run/Sprint 分组，蹲伏用 `CrouchAnimSet.Starts` 不分组。

**② 定相位（Setup）**
- `Sequence Evaluator`（`SetExplicitTime` 直接设帧）vs `Sequence Player`（只能从 0 累积）。
- 两个易混量：`StartDir`（角度，选动画）vs `RotationAlpha`（0–1，旋转完成度，反查相位）。
- `RotStartTime = GetTimeAtRotation(RotationAlpha)`；`DistStartTime = GetTimeAtDistance(1)`（1 cm ≈ 动画开头）。
- 首次进 Start 用 `DistStartTime`；已在 Start 再 Setup 用 `RotStartTime` 恢复旋转进度。

**③ 逐帧推进（Update）**
- **距离匹配管时间**：`AdvanceTimeByDistanceMatching`，把动画时间推到"根位移增量 ≈ 本帧真实位移"，位移 0 则停。
- **旋转匹配管朝向**：`CustomRotationYaw = EntryRotYaw + TargetAngle × MappedRot + 加速度修正`。模板（90°/180°）只是粗分档，`TargetAngle = 真实输入角` 把模板转身**等比缩放**到实际角度。
- **`FirstStepAlpha`**：时间 `[0.1, 0.4]` → `[0,1]`，控制 Stride/Orientation Warping 逐步放开，先保留原始落脚。
- 核心分工：**距离曲线管时间、旋转曲线管朝向**，二者经"当前动画时间"耦合；转身节奏由距离匹配决定，旋转匹配不是节拍器。

**坑**：四向起步 + 开 `HasRotatingStarts` 却缺旋转资源会请求空；`GetTimeAtDistance(1)` 的 1 不是"第一步"；起步可提前切 Loop（第一脚完成且旋转偏移 ≥50° 或速度方向已变）。

---

### 2. 循环（Loop）

**一句话**：Loop 用**速度匹配**而非距离匹配——没有目标距离，只调播放速率让每个周期平均位移速度 = 实际速度，从而保持脚相连续。

**① 选资源**
- 步态（`EffectiveSpeedScaler` 阈值 + 滞回）+ **速度卡迪纳尔方向**（不是输入方向）→ 选**一个**离散姿势锚点 `FKLSLoopAnim`，不是四向 BlendSpace 连续插值。

**② 调步频**
- `SetPlayRateToMatchSpeed`：`PlayRate = 实际速度 / 动画原始速度（根位移÷时长）`，clamp 到 `PlayRateClamp`。
- 只改时间推进**斜率**，不重设相位 → 左右脚相位连续。用 `Sequence Player + PlayRate`。

**③ 补朝向**
- Orientation Warping 填四向锚点间的角度残差：前向残差 = `LocalVelocityDirection`，右向 −90°，左向 +90°，后向 +180°。

**④ 扇区滞回**
- 基础扇区前向 ±45°、后向 ±135° 外，Dead Zone 10°，前/后扇区扩一倍 → 从侧向进前向需 ±55°，已是前向则保持到 ±65° 才离开；避免 45°/135° 附近抖动反复换资源。

**核心边界**：瞬态（起步/停止/Pivot）用 `Sequence Evaluator` + 距离/旋转曲线；循环用 `Sequence Player + PlayRate`——这是两类动画工具的分工，不是可互换的偏好。

**坑**：四向资源必须对齐左右脚相位/循环起点/周期时长/根位移速度/重心高度（Warping 救不了）；`PlayRateClamp` 是动画与移动参数的设计合同（太窄→脚滑，太宽→节奏失真）；按输入方向切循环会错（反向由 Pivot 表达）。

---

### 3. 停止（Stop）

**一句话**：停止动画的距离曲线是**负值**——0 是停止点、负值表示离停止点还差多少，与起步/循环的正值相反。

**① 选资源**
- **速度卡迪纳尔方向**（松开输入无可靠输入方向）+ 步态 + **左右脚**（`LeftFootUpCurve`/`KLSLeftFootUp`）+ 蹲伏。
- `UsePerFootStop` 时每向有左右脚两套。

**② 预测停止距离**
- `PredictGroundMovementStopLocation`：制动摩擦（`GroundFriction`×`BrakingFrictionFactor`）+ `BrakingDecelerationWalking` 做速度→0 减速外推，再 trace 收敛到可站立表面。

**③ 距离匹配**
- Setup：`SetExplicitTime(GetTimeAtDistance(-StopDistance))`。
- Update：每帧重算预测，`DistanceMatchToTarget`；到结尾前 0.2s 写 `StandingStopBlendToLoop`；检测到重新加速立即停止匹配（改走 Start/Pivot）。

**坑**：负值曲线别用正思维读；停止终点由 Movement Component 预测决定（改制动/摩擦/最大速度要重新核对曲线）；`UsePerFootStop` 开启却只配一套资源会选错。

---

### 4. 折返（Pivot）

**一句话**：反向输入 ≠ 停止后起步——把"仍沿旧方向滑行"和"已输入新方向"拆成 **Pre-Pivot** 和 **Post-Pivot**。

**① 判断**
- 移动组件 `PhysWalking()` 判速度与加速度近似反向；`bCanPivot` 防非法状态持续 Pivot。

**② Pre-Pivot（消化旧速度）**
- 用局部加速度方向定目标角 + 输入卡迪纳尔方向，取其**反方向**作旧速度方向。
- 选 `FKLSPivotAnimSet`（按站立/蹲伏、步态、靶向、左右脚、资源可用性）；预测"速度换向点"距离定位并推进；带旋转动作同时 Rotation Matching。

**③ Post-Pivot（建立新方向）**
- 到 `PivotTime` 后进入；**硬 Pivot 从该时间点、软 Pivot 从 0 秒**开始；以新输入方向选动作按位移推进 → Loop。

**④ 软 Pivot 回退**
- 无专门旋转/四向 Strafe Pivot 时：旧方向 Stop 作 Pre + 新方向 Start 作 Post，是资源不足时保连续的设计，不是失败。

**坑**：资源选择同时需要旧速度方向 + 新输入方向（不可互换）；`PivotTime` 是 Pre/Post 切点（软 Pivot 无此切点）；模式切换/蹲伏/Linked Layer 切换会清除 Pivot 资格。

---

## 二、ALS

### 1. 相机系统（Camera）

**一句话**：相机是 3C 中独立一环，本质是"玩家看哪里（Control Rotation）"与"角色朝哪里（角色朝向）"**解耦**。

**① 3C 职责边界**
- Character（移动/朝向/姿态）、Camera（取景/跟随/防穿墙）、Control（输入→旋转）；**Control Rotation 是三者交汇**，既决定相机朝向，又作为动画侧 `ViewState` 输入反哺脊柱/头部。

**② 核心机制 = 锚点 + 两个延迟 + 防穿墙**
- **Pivot 锚点**：角色位置，`PivotLagSpeed` 平滑，隔离贴地/上下台阶高频抖动。
- **旋转延迟**：目标旋转 = Control Rotation，`CameraRotationLagSpeed` 平滑；`CalculateAxisIndependentLag` 拆局部坐标轴（俯仰/偏航/翻滚）分别 `FInterpTo`。
- **位置延迟**：期望位置 = 锚点 + 由旋转推导的偏移（俯仰定高低），`CameraLagSpeed` 平滑 → 弹簧臂软跟随。旋转延迟与位置延迟是两个独立手感旋钮。
- **Camera Behind Character**：移动时目标 Yaw 回正到移动方向后方。

**③ 防穿墙与视角切换**
- 锚点向期望位置 Trace，命中拉近留余量；`CameraTraceChannel`；忽略角色自身；拉近后仍过位置延迟。
- 第一人称切头部 Socket，FOV 在 `ThirdPersonFOV`/`FirstPersonFOV` 过渡。

**④ ALS 两版实现**
- v4/Community：全局 `APlayerCameraManager` 重写 `CustomCameraBehavior`。
- Refactored：角色上的 `UAlsCameraComponent` + `UAlsCameraSettings` 数据资产，`TickComponent` 分阶段（锚点→旋转→距离→位置→FOV）→ `GetViewInfo`。支持移动平台、参数集中可被动画驱动（瞄准拉近、蹲伏降相机）。

**⑤ 与动画的耦合**
- `ViewYaw = ViewRot.Yaw − CharRot.Yaw`；`PitchAmount = 0.5 − ViewPitch/180` → 驱动 Aim Offset/脊柱/头部。
- **RotationMode** 决定耦合程度：`VelocityDirection`（朝速度，完全解耦）/ `ViewDirection`（朝视角）/ `Aiming`（限制在视角附近）。

**坑**：相机带动角色转身→查 RotationMode 是否误切 ViewDirection；瞄准头/脊柱过度偏转→ViewState 是否与 Aim 资源烘焙偏转**叠加（双重旋转）**。

---

### 2. 姿势混合（Overlay / Layering / 分层）

**一句话**：ALS 的姿势合成不是一次上半身混合，而是四层解耦——**语义选择（Tag）→ 姿势资源（Linked Anim Layer）→ 区域曲线分层（LayeringState）→ 最终约束（Control Rig）**。

**① 先区分易混概念**
- `OverlayMode`（哪套附加姿态，Gameplay Tag）vs `RotationMode == Aiming`（朝向规则，不同的事）。
- `LayeringState`（区域混合权重，读 `Layer*` 曲线）vs `PoseState`（基础姿势权重，读 `Pose*` 曲线）vs `ViewState/SpineState/HeadState`（视角分配）。

**② 四层数据链路**
1. **状态选择**：`SetOverlayMode` → 复制/RPC → `OnOverlayModeChanged` → Tag→AnimBP 类映射 → `LinkAnimClassLayers`。Tag 只选语义，Linked Anim Layer 类负责实现姿势。
2. **资源输出**：`ALI_Overlay` 统一接口，各 `AB_Als_*`（Default/Rifle/Box/Injured…）实现，内部复杂度可完全不同。
3. **曲线分层**：`AB_Als_Layering` 通用合成器。
4. **最终约束**：`CR_Als`（Spine/Hand/Foot IK）。

**③ PoseState（让 Overlay 跟随基础姿势）**
- 读 `PoseGrounded/PoseInAir/PoseStanding/PoseCrouching/PoseMoving/PoseGait` 曲线，是**动画姿势的权重**（可处于中间值），而非 Character Tag 的 0/1 副本。蹲伏切换看连续权重，不是 Stance Tag。

**④ LayeringState（身体区域如何叠加）**
- 区域：Head / ArmL / ArmR / HandL / HandR / Spine / Pelvis / Legs。
- 每区域三条通道：**Override**（`Layer*`，替换结构）/ **Additive**（`Layer*Additive`，叠加动态）/ **Slot**（`Layer*Slot`，注入 Montage）。曲线来自动画资源，可随时间变化。

**⑤ 三种合成方式**
- **Override**：区域绝对姿势替换基础（建结构，如持枪双臂）。
- **Additive**：差值叠加到基础（保动态，如受伤偏斜/瞄准微调），需正确参考姿势。
- **Slot**：Montage 插入位置，同一 Montage 可在不同区域不同表达（手臂 Override + 脊柱 Additive）。

**⑥ Local Space vs Mesh Space**
- 手臂用 **Local Space**（继承脊柱旋转）；**Mesh Space** 相对网格保持方向。Arm 的 Mesh 是默认路径，`LayerArm*LocalSpace` 满权重时 Mesh 关闭——是二选一，不是两个独立强度。

**⑦ Layering 图七阶段**
1. 缓存 Pose（`Save/Use Cached Pose`，防重复求值）
2. 参考姿势（`StandPose × StandingAmount + CrouchPose × CrouchingAmount`）
3. 提取 Locomotion 动态 Additive（`CurrentLocomotion − ReferencePose`）
4. 区域 Override + Additive
5. 独立 Hands 层（防 Arm Additive 污染手指/武器骨/虚拟骨）
6. 区域 Slots
7. **Curves Blend** 恢复稳定曲线（`FAlsAnimNode_CurvesBlend`，防"曲线驱动混合又被混合污染"的反馈）

**⑧ 瞄准分配（避免双重旋转）**
- `HeadBlendAmount = (1−ViewBlock) × (1−PoseAiming)`；`SpineBlendAmount = (1−ViewBlock) × PoseAiming`。`PoseAiming` 决定视角 Yaw 交给脊柱还是头部。

**⑨ 武器重量感（Fundamentals 02）**
- 重量感不是物理质心，而是骨盆/根骨、脊柱肩胯、站距步幅脚步节奏、起停转向、加速制动参数多信号共同作用。
- **判断标准**："是否改变脚步/骨盆/根骨/整体惯性？" 是→专用全身动作集；否→上半身层/Additive。
- 三级实现：共享移动 + 上半身叠加 / 替换层与参数 / 专用全身数据库（重武器、盾牌、双手近战）。

**坑**：下半身被 Overlay 覆盖→查 `LayerPelvis`/`LayerLegs` 权重过高；瞄准双重旋转→Aim 资源已烘焙 Yaw 又被 Control Rig 分 Spine Yaw；手指/武器抖→先查 Hand Layer/虚拟骨再查 Hand IK。

---

## 三、UE5 现代动画节点

> 每个节点一句话 + 关键词。按「修改对象」记职责：**选择 → 时间 → 局部骨骼 → Mesh Root/Root Motion → 过渡**，不要让两个模块反复修同一对象。

### 接触与 IK

**Foot Placement**：把脚接触当**连续状态**求解（落脚点/朝向/Plant-Swing 切换/骨盆补偿），不是"射线 + Two Bone IK"。
- 跨帧状态 `FLegRuntimeData`：种植平面 `PlantPlaneRS`、锁定变换、状态机 `PlantType`、弹簧。
- `WantsToPlant` 四条件（想不想锁）vs `DeterminePlantType` 迟滞（实际锁没锁）。
- `LockType`：`PivotAroundBall`（绕脚掌）/`PivotAroundAnkle`/`LockRotation`。
- swing/twist 拆分 + `AnkleTwistReduction`；`SolvePelvis` 骨盆补偿（水平重平衡 + 竖直可达范围）。
- 不碰腿链 → 下游 Leg IK；旧 Foot Lock/Foot Offset/Pelvis Offset 三个独立修正器被并成一个耦合求解。

**Leg IK**：执行已确定的脚目标，把大腿—小腿—脚解算回腿链；只回答"怎样够到"，不回答"脚该踩哪"。
- 组件空间骨骼控制；Orient（对齐方向）→ Reach（够到）→ Twist（膝盖扭转校正）。
- 两骨闭式解（余弦定理）vs FABRIK（>3 骨）。
- 膝盖弯曲方向从 FK 动画提取 + `HingeRotationAxis` 兜底（腿伸直时防翻面）。
- 目标不可达→**打直不拉长**（长度固定）；`SoftPercentLength` Soft IK 防伸直 pop。
- 顺序：Foot Placement（规划）→ Orientation Warping（方向）→ Leg IK（执行）。

**Full Body IK**：Control Rig 里的 PBIK 全身求解，多 Effector 共同影响骨盆/脊柱/四肢。
- 新 PBIK（Position Based：带质量刚体 + Pin/Joint 约束）vs 旧 Jacobian（`FRigUnit_FullbodyIK`，已 Deprecated）。
- 四个让位旋钮：**质量**（骨段长度）定谁重、**Stiffness** 定谁让、**Rotation Limit** 定让多少、**Preferred Angle** 定往哪让。
- `RootBehavior`：`PrePull`（整体预拉，默认）/`PinToInput`（根锁死）/`Free`。
- 与 Leg IK 边界：单链用 Leg IK；多末端需骨盆/脊柱让位用 FBIK。

### 姿势 Warping

**Orientation Warping**：把"下半身转向实际移动方向、上半身保持面向"拆成正反旋转，摊到根骨/脊柱/IK 脚。
- 一个残差角 A 分摊：根骨 `+A·d`、脊柱 `−A·d`、IK 脚根 `+A·(1−d)`、IK 脚 `−A·(1−d)`（d 默认 0.5）→ 下肢净转 A、上半身净转 0。
- 脊柱权重"每条路径和为 1"。
- Manual（直接给角）vs **Graph**（根运动方向 vs 移动方向反推，还会旋转根运动写回属性流）。
- 90° 反转拦截；反向补偿（保留自然根运动转角，插值只平滑残差）。
- 不碰腿链 → Leg IK；常驻 Motion Matching Blend Stack 内层。

**Stride Warping**：按实际速度/动画参考速度之比拉开/收窄跨步，**改空间步幅不改时间**。
- `StrideScale = LocomotionSpeed / RootMotionSpeed`；Graph 模式同步缩放根运动写回（双重缩放坑）。
- 缩放原点 = 髋正下方（相对身体拉长，只沿步幅方向）。
- 骨盆下拉防过伸（`PelvisIKFootSolver`）；大腿旋转补偿 + FK 腿长钳制。
- 围绕参考速度小幅修正（±20%），跨档换资产；与 Play Rate 互补（保节奏）。

**Slope Warping**：整段步态适配坡面法线，必要时下拉骨盆容纳脚部高度差。
- **复用胶囊 `CurrentFloor`**（不自己 trace），没地面退回平地。
- `FindBetweenNormals(IKFootRoot Z 轴 → 坡面法线)` 整体旋转下半身，不是逐脚贴地。
- `bPullPelvisDown` 下拉骨盆解决腿过伸；`bKeepMeshInsideOfCapsule` 留胶囊内。
- 顺序：Slope → Foot Placement → Leg IK；与 Orientation 各管一摊（坡 vs 方向）。

### 根运动与轨迹

**Steering**：逐步引导已选动画的根运动向目标轨迹靠拢，处理**连续移动的跟随意图**。
- 只改根运动旋转（yaw），不动骨骼、不改位移大小。
- 预测缩放（采样未来 `AnimatedTargetTime` 的 `PredictedYaw` 算 Ratio）+ 弹簧补差（临界阻尼追缩放后残差）。
- 需要 `CurrentAnimAsset`/`CurrentAnimAssetTime` 瞬态引脚（未接→只剩弹簧、慢半拍）。
- 与 Motion Warping 分工：连续意图 vs 精确到达。

**Motion Warping**：组件 + NotifyState + Root Motion Modifier 三件套，在 Warping Window 内对齐 Montage 根运动到场景目标。
- `UMotionWarpingComponent`（WarpTarget 表）/ `UAnimNotifyState_MotionWarping`（窗口）/ `URootMotionModifier`（模板）。
- `WarpTargetName` 按名字查表——**目标在组件不在动画**。
- 窗口是修正量预算（越短单帧越狠）；`MaxSpeedClampRatio` 限速。
- Skew Warp = Scale × ShearXAlongY × ShearXAlongZ（保留横向摆动）；Simple Warp 已 Deprecated。
- **必须开 Root Motion**（入口 `ProcessRootMotionPreConvertToWorld`）。

**Offset Root Bone**：管理 Mesh Root 相对胶囊的**内部偏差**，让网格短暂保留动画惯性。
- `偏移 = ComponentTransform − SimulatedTransform`。
- 六模式 = 三开关组合（抵消组件位移 / 消费动画根运动 / 插值释放）。
- `Accumulate` 不会自己回来 → 必须切 `Release`/`Interpolate`；平移旋转可分别设。
- Graph 模式需上游根运动属性流；给 Orientation Warping 当参考空间（`FRootOffsetProvider`）。

### 时间

**Distance Matching**：用 Distance Curve 把"动画播到哪一帧"变成**距离反查**，空间事件与游戏距离对齐。
- 三件套：Distance Curve（数据）/ Sequence Evaluator（载体，Explicit Time）/ Anim Node Function（驱动）。
- 二分查找 + 线性插值反查；`DistanceMatchToTarget`（绝对）vs `AdvanceTimeByDistanceMatching`（相对，换 effective play rate）。
- 曲线符号约定：停止点前负后正（负值反查）。
- 循环用 Play Rate（`SetPlayrateToMatchSpeed`），距离匹配给一次性动画（起/停/转/落地）。
- `PlayRateClamp` 是"绝对不滑"与"别像慢动作"的取舍。

### 查询数据与资源选择

**Pose History**：为 Motion Matching 保存跨帧骨骼姿势历史，提供"刚才怎样运动"的查询证据。
- 环形缓冲 `PoseCount × SamplingInterval`；时间 0 = 当前 / 负 = 过去 / 正 = 未来。
- 与 Cached Pose 区别：跨帧历史 vs 同帧复用。
- `GetTransformAtTime` 二分插值，速度 = 两次取样差分。
- 必须**常驻路径每帧求值**（否则过期姿势）；`bResetOnBecomingRelevant` 清历史；`CollectedBones/Curves` 按 Schema 列。

**Trajectory**：过去 + 预测未来的位置/朝向/速度采样序列，是 Trajectory Channel 的输入。
- 采样点 = {Position, Facing, TimeInSeconds}；负 = 历史 / 0 = 当前 / 正 = 预测，同一世界坐标。
- 历史是**记**出来的，预测是**算**出来的（前向积分复用 CMC 摩擦/制动/最大速度）。
- 移动平台修正（旋转平台须 `WithRotation`）。
- 消费端 Trajectory Channel 展开 Position/Velocity/Facing 子通道逐点比。
- 5.6 起 `FPoseSearchQueryTrajectory` → `FTransformTrajectory`；**输入不平滑 → 频繁重选**（先平滑输入再堆数据库）。

**Chooser**：规则表驱动的对象选择，用上下文缩小可选资源；不混合 Pose、不算 IK。
- `UChooserTable` = 上下文声明 + 列（过滤/评分/输出）+ 结果行 + Fallback。
- 属性链解析（编译期展开偏移量/函数链）；过滤列读不到值→**静默透传**（不报错）。
- `FAnimNode_ChooserPlayer` 内部就是一个 BlendStack。
- 典型用：先缩小 PoseSearch Database 候选集；`EvaluationFrequency`（OnUpdate/OnLoop）。

**Proxy Table**：资源**间接引用**，运行时按上下文解析成最终动画/数据库/Montage。
- `UProxyAsset` 占位符（Guid 身份 + Type + ContextData）vs `UProxyTable` 映射（Proxy → 值）。
- 键是 **Guid**（改名移动不破坏，撞车报 Error）；值可以是直接引用/Chooser 表/再套 LookupProxy。
- 继承先到先得（子覆盖父）。
- 与 Chooser：Chooser 偏条件选择，Proxy 偏资源替换，可互相嵌套；**资源恒定就别用**（多一层间接）。

### 动态姿势过渡

**Blend Stack**：面向动态姿势流的混合基础设施，持续吸收 Motion Matching 频繁改选的候选。
- 一叠候选（索引 0 最新，权重顶向下递减，老的淡出剪枝）vs 两路 blend。
- `BlendTo` 三判断：栈空直进 / 未淡入完顶掉 / 正常压栈。
- `MaxActiveBlends` 溢出 → 烤成静态姿势（`bStoreBlendedPose`）。
- 子图 Sample Graph（`FAnimNode_BlendStackInput` 替身）让每个候选先被修正（Orientation Warping/Steering 放内层）。
- `bUseInertialBlend` 把接缝外包；不支持 Montage。

**Inertialization**：切换瞬间记录运动趋势、让残差逐帧衰减，**来源动画可停止求值**。
- 切换帧采样"旧−新差 + 差的变化速度"，之后每帧只求新姿态（旧动画退场省成本）。
- 残差 = 新姿态 + 方向 × F(差值, 速度, 时间, 时长)；**五次多项式**（值/一阶导连续，终点值/一阶/二阶归零）。
- 靠 Request 触发（状态机 Blend Logic = Inertialization 或 Linked Anim Graph），没请求永远 Inactive。
- 节点必须在请求的**祖先路径**（rootwards）；`deficit` 机制防 pose melting；根运动一起惯性化。

**Dead Blending**：与 Inertialization 同族的残差过渡，用 Blend Curve 塑造残差衰减形状。
- **外推旧动画**（幽灵姿势）再交叉淡化 vs Inertialization 往目标拽。
- 抓速度（前两帧差分）按半衰期指数衰减积分（收敛到 `p0 + v0/C`）。
- 半衰期自适应：顺路慢衰减、逆路快掐断。
- 衰减形状可控（`DefaultBlendMode`/`CustomBlendCurve`）→ 适合反复启停的动态姿势层（如 Aim Offset）。
- 落地顺序：先 Inertialization 建基线，需要自定义衰减/滑行手感再上 Dead Blending。

---

## 附：来源索引

- KAI：[起步](./KAI/04_起步_靶向与非靶向的动画选择.md)、[循环](./KAI/05_循环_步态与方向的移动表现.md)、[停止](./KAI/06_停止_距离匹配与左右脚选择.md)、[折返](./KAI/07_Pivot_反向输入的预转向与衔接.md)、[输入与朝向](./KAI/03_地面移动的输入与朝向模式.md)、[状态交棒](./KAI/17_地面状态_过渡条件与交棒.md)
- ALS 相机：[Camera 目录](./Camera/README.md)
- ALS 姿势混合：[瞄准与 Overlay](./ALS/07-瞄准与Overlay.md)、[Layering 设计](./ALS/07.1-AB_Als_Layering设计.md)、[姿态叠加与分层](./Fundamentals/01-姿态叠加与动画分层.md)、[武器姿态与重心](./Fundamentals/02-不同武器的姿态与重心如何实现.md)
- UE5 现代节点：[UE5现代动画节点 目录](./UE5现代动画节点/README.md)
